[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.20"

# Remappings for OpenZeppelin
remappings = ["@openzeppelin/=lib/openzeppelin-contracts/"]

# Optimizer
optimizer = true
optimizer_runs = 200

[fmt]
line_length = 100
tab_width = 4
bracket_spacing = true
